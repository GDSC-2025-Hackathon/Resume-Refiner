// Main entry point for the React app.
// It tells React to render the App component inside the root <div> in public/index.html.
// This is where React connects your components to the real browser DOM.